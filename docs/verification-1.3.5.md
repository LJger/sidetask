# SideTask 1.3.5 发布验证

## 发布内容

本次包含提交 `cb0939f` 的窗口边框修复与桌面测试调整。应用及浏览器预览版本统一为 1.3.5，继续使用 v3 数据格式。完整更新内容见 [发布说明](release-1.3.5.md)，修复原理与长测细节见 [窗口边框验证记录](verification-window-frame.md)。

## 已完成检查

- 71 项 JavaScript 测试和 20 项 Rust 核心测试通过。
- Rust 格式检查通过。
- 在 WSL 使用 cargo-xwin 完成 Windows x64 Release 交叉构建，用于本地验证。
- Windows 11（10.0.26200）、WebView2 153.0.4234.48 上的桌面自动化通过：原生启动、保存、置顶开关两种状态各 40 轮展开收起（含动画中反转）、4 次焦点反转、2 次 Alt+F4、草稿保护、并发写入、失败保存恢复、隐藏启动与重启持久化。
- 2,674 次原生窗口样式采样中，系统边框样式出现 0 次。

## 验证范围

本地验证使用独立应用标识与临时数据目录，与运行中的正式版并存。真实鼠标拖动、多显示器与 DPI、Windows 10、安装升级流程以及 Edge 收藏夹和 Nexus 等程序的交互未在本轮覆盖。

升级前请保存草稿并从托盘退出旧版。

## 发布文件

发布文件由 Windows 构建工作流生成，校验信息在发布完成后补充。
